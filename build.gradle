apply plugin: 'cpp'

model {
    components {
        primes(NativeExecutableSpec) { 
            sources {
                cpp {
                    source {
                        srcDir "src"
                    }
                }
            }
        }
    }
}
